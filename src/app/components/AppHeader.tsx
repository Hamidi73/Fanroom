// Header for focused pages (auth, rooms, room detail, profile, admin). Same top
// nav as the rest of the site, without the browse sidebar.
import { TopNav } from "./TopNav";

export function AppHeader() {
  return <TopNav />;
}
